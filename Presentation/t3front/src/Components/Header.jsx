const Header = (props) => {
    
    const header = (
        <div className="header">
            {props.home ? 
            :
            }
        </div>
    )
    return header;
};

export default Header;